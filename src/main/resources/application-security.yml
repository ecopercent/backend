# JWT
jwt:
  secret-key: ${JWT_SECRET-KEY}
  access-token-expiry-date: 30000000
  refresh-token-expiry-date: 60000000

# KaKao OAuth2
kakao:
  kapi-uri: https://kapi.kakao.com
  user-info-uri: /v2/user/me